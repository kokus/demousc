{% if title %}
  <h2 id="letter-{{ title|striptags|trim|lower }}">{{ title }}</h2>
{% endif %}
{% for row in rows %}
  {%
    set row_classes = [
      default_row_class ? 'views-row',
    ]
  %}
  {{- row.content -}}
{% endfor %}
