uuid: 584fda19-66ee-4abe-99b2-666100b1fe85
langcode: en
status: true
dependencies:
  config:
    - field.storage.media.field_link
    - media.type.image
  module:
    - link
id: media.image.field_link
field_name: field_link
entity_type: media
bundle: image
label: 'Link Image to a Page'
description: 'Only applies to <strong>Body Enhanced</strong> and <strong>Sidebar</strong> images.  Leave this field BLANK if image is NOT linked. Enter a partial URL to link this image to an internal page (e.g. node/1234). For an external page, use an absolute URL (e.g. http://www.example.gov/some_enternal_page).  '
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  title: 0
  link_type: 17
field_type: link
